#ifndef MODLIB_RAYLIB_TYPEDEF_H
#define MODLIB_RAYLIB_TYPEDEF_H

/**
 * mimic raylib::Color
 */
typedef struct Color {
    unsigned char r;
    unsigned char g;
    unsigned char b;
    unsigned char a;
} Color;

#endif // MODLIB_RAYLIB_TYPEDEF_H